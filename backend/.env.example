# 后端环境变量示例:复制为 backend/.env 并填写(.env 已被 gitignore,不会进入仓库)

# LLM API Key(任意 OpenAI 兼容接口,本项目以阿里云百炼 DashScope 的 Qwen 验证)
# 优先级:「模型配置」页保存的密钥 > 此环境变量
LLM_API_KEY=

# 地图密钥回落值(推荐登录后在「地图管理」页维护并持久化到数据库,此处仅作兜底)
MAP_BAIDU_AK=
MAP_AMAP_KEY=
MAP_AMAP_SEC=
MAP_TDT_KEY=
